1. Read an integer `n` from user input.

2. Check if `n` is an odd number:
   a. If true, print 0.
   b. If false:
      - Calculate `2` raised to the power of `(n//2)` using the exponentiation operator.
      - Print the result.
